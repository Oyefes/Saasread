import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Discover",
  description: "page",
};

const Discover = () => {
  return <>Discover</>;
};

export default Discover;
